사전작업
node.js 설치
 npm (패키지 관리도구) == 파이썬 pip

vite : 빌드도구
 개발 중 작성한 여러 개의 파일을 하나 또는 몇 개로 묶는 도구

ESLint : 문법 검사

npm create vite@latest
npm install tailwindcss @tailwindcss/vite
npm install react-router-dom
npm install react-icons
npm install react-redux @reduxjs/toolkit
npm install axios

1. vite 기반으로 react 프로젝트 생성하기 
npm create vite@latest 프로젝트명
  press r + enter to restart the server
  press u + enter to show server url
  press o + enter to open in browser
  press c + enter to clear console
  press q + enter to quit

npm run dev : 작성하는 코드가 실시간으로 반영됨(개발 시 사용)

개발 종료 후 build

npm run build : 서버에 올려서 사용자들에게 웹페이지로 배포될 파일들을 코드를 모아 컴파일
 dist 폴더 생성


npm run preview : build를 통해 나온 결과물을 테스트하는 단계
 http://localhost:4173/

소스코드
jsx (javascript xml)
tsx (typescript xml)


라우팅 : 링크(페이지 이동)

<a href='index.html'>인덱스</a>

컴포넌트

링크관련

a : html 태그
 - 새로고침되면서 화면의 깜빡임 발생

NavLink, Link : React-router-dom
 - 새로고침 없이 화면이 변경됨
 
● NavLink : active 클래스가 자동으로 부여
 - 상단의 URL 과 NavLink의 to 속성값 일치시
● Link : active 클래스 없는 상태

Outlet
● 중첩라우팅에서 자식 라우트를 렌더링할 위치를 지정
● 공통으로 사용할 레이아웃 지정

리액트에서는 use~~~ : 훅
훅 : React가 제공하는 특별 함수
 - React의 기능을 사용할 수 있음
 - 컴포넌트의 최상위에서만 호출 가능
 - React의 상태, 생명주기 등의 기능과 연결

useSearchParams / useLocation

useLocation
● 현재 페이지의 전체 URL 정보를 가져올 수 있는 훅
● pathname(현재경로)
● search(쿼리스트링) : 문자열 형태

useSearchParams
● 현재 페이지의 쿼리 스트링을 개별항목으로 가져오거나 수정할 수 있는 훅

useParams
● 동적으로 생성되는 파라미터 가져오기

/account/login
/account/register
/account/profile/3 + GET : 3번유저의 profile 가져와

컴포넌트 생명주기 : useEffect() 훅
마운트(화면에 그려짐), 업데이트(상태(state), 속성(props) 변경되어 다시 렌더링), 언마운트(화면에서 사라짐)

서버에서 데이터 가져오기(json)


구글 아이콘
폰트 어썸

성능 최적화
useRef()

React Compiler
=> 부모가 렌더링 시 관련없어도 자식 렌더링 같이 됨 => 관련없으면 렌더링 안되도록 해줌
=> useMemo() : 계산결과 기억, 판단 후 적절하다고 판하다고 판단이 되면 이 부분도 해결해줌
=> useCallback() : 함수 기억(부모가 자식에게 함수를 props 넘길때)


useCallback() : 함수 기억(부모가 자식에게 함수를 props 넘길때)
리렌더링 시 함수는 새롭게 생성됨

useId() : 각 컴포넌트 인스턴스마다 고유한 ID 를 제공
          언마운트 되기 전까지 동일한 ID 유지

useState => useReducer

useReducer
 - 관리해야 할 상태 로직이 복잡할 때 사용
 - 상태를 어떻게 변경할지 관리

useContext() : 상태를 여러 컴포넌트에 어떻게 공유할지 관리
1. Context로 사용할 객체 생성
2. 생성된 Context 객체를 통해 Provider 생성
3. 상태를 공유할 자식 컴포넌트를 Provider로 감싸기
4. 자식 컴포넌트에서 공유된 값 사용

redux
- useReducer, useContext 대체
- 리덕스 툴킷(리덕스 + 리덕스의 복잡한 설정 기능 보완)

리덕스 툴킷
화면에서 이벤트 발생 => dispatch() => 리듀서 호출 => 리듀서 액션 분석 후 상태 변경 => 새로운 상태 반환
=> 화면렌더링

immer : 알아서 불변성을 지켜줌
원본 수정 가능
push()


외부에서 데이터 가져오기(내부서버로 대체 예정)
https://jsonplaceholder.typicode.com/posts
 => fetch(), axios()

수정 => 수정하는 대상을 가져와서 화면에 보여주고
내용 수정 후 수정 => PUT